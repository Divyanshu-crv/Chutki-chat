{
  "entities": {
    "User": {
      "title": "User",
      "description": "A user profile in the chat application.",
      "type": "object",
      "properties": {
        "uid": { "type": "string", "description": "The unique ID of the user." },
        "displayName": { "type": "string", "description": "The display name of the user." },
        "photoURL": { "type": "string", "description": "The URL of the user's profile photo." },
        "email": { "type": "string", "description": "The email address of the user.", "format": "email" },
        "role": { "type": "string", "description": "The role of the user.", "enum": ["admin", "user"] },
        "createdAt": { "type": "string", "description": "The timestamp when the user was created.", "format": "date-time" }
      },
      "required": ["uid", "displayName", "email", "role"]
    },
    "Message": {
      "title": "Message",
      "description": "A message sent in the chat.",
      "type": "object",
      "properties": {
        "text": { "type": "string", "description": "The content of the message." },
        "senderId": { "type": "string", "description": "The UID of the sender." },
        "senderName": { "type": "string", "description": "The name of the sender." },
        "senderPhoto": { "type": "string", "description": "The photo URL of the sender." },
        "timestamp": { "type": "string", "description": "The timestamp when the message was sent.", "format": "date-time" },
        "isAiResponse": { "type": "boolean", "description": "Whether the message is an AI response." },
        "isThinking": { "type": "boolean", "description": "Whether the AI used thinking mode." }
      },
      "required": ["text", "senderId", "senderName", "timestamp"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "User",
      "description": "Collection of user profiles."
    },
    "/messages/{messageId}": {
      "schema": "Message",
      "description": "Collection of chat messages."
    }
  }
}
